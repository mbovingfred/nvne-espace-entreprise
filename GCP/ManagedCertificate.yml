apiVersion: networking.gke.io/v1
kind: ManagedCertificate
metadata:
  name: mcert-nvne-fr
  namespace: nvne
spec:
  domains:
    - nvne.fr
    - www.nvne.fr
    - admin.nvne.fr
    - accounts.nvne.fr
